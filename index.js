/**
 * @format
 */

import {AppRegistry} from 'react-native';
import InitialStack from './src/navigation/InitialStack';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => InitialStack);
